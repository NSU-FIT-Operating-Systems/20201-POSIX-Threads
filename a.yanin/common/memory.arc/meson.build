memory_arc_deps = [
  dependency('threads'),
  modules['collections'],
]

modules += {
  'memory.arc': declare_dependency(
    include_directories: 'include',
    dependencies: memory_arc_deps,
  ),
}
